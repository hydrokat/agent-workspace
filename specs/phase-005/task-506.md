# Task 506: Invitation Flow End-to-End Tests

## Goal
Validate the complete admin-to-user invitation and reset lifecycle.

## Requirements
- Write Playwright tests for:
  - Admin searching and adding an existing user.
  - Admin inviting a new user by email.
  - Invited user logging in with the autogenerated password (simulated or retrieved from DB/logs).
  - Invited user being forced to reset password.
  - Invited user accessing the dashboard after reset.

## Implementation Details
- **Location**: `tests/e2e/admin-invitation.spec.ts`.
- **Helpers**: Use Supabase admin client in tests to verify DB state.

## Verification
- Run full test suite and ensure zero regressions.
