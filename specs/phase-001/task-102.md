# Task 102: Next-Auth & Supabase Integration

## Description
Configure Next-Auth to handle authentication and ensure users are automatically provisioned in the `profiles` table upon first login.

## Objectives
- [ ] Configure `auth.ts` with Next-Auth v5.
- [ ] Implement a `signIn` callback to check/create the user profile in Supabase.
- [ ] Sync the user's committee and role into the session/JWT.
- [ ] Protect initial routes using middleware.

## Agent Assignment
- **Agent**: `senior-backend-dev` / `frontend-dev`
- **Model**: Gemini 2.0 Flash

## Status
- [ ] Pending
